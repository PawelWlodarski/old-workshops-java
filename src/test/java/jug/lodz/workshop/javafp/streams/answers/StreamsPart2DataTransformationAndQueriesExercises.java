package jug.lodz.workshop.javafp.streams.answers;

/**
 * Created by pawel on 13.06.16.
 */
public class StreamsPart2DataTransformationAndQueriesExercises {
}

//write generic filter method

//flatmap isValid do filtrowania-> i produkuje stream!! stream !!!


/** count number of elements
 * int count = menu.stream()
 .map(d -> 1)
 .reduce(0, (a, b) -> a + b);
 */
